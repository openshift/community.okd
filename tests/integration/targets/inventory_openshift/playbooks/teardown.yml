---
- name: Delete all resources
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Delete resources namespace
      community.okd.k8s:
        api_version: v1
        kind: Namespace
        name: inventory-test
        state: absent
